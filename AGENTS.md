# FLOW PM KIT — Project Rules

이 저장소는 PM/PO 기획과 프로토타이핑을 위한 재사용 가능한 AI 작업 시스템이다.

## 기본 작업 순서
1. 사용자의 목적과 문제를 먼저 정의한다.
2. 사실 / 가정 / 확인 필요를 분리한다.
3. PRD, 제안서, 요구사항서, 정책서, 보고서 중 적절한 문서를 만든다.
4. 모바일 화면을 먼저 설계한다.
5. 모바일 정보구조를 유지하며 웹으로 확장한다.
6. 필요한 경우 클릭 가능한 프로토타입을 만든다.
7. 사수 리뷰와 QA를 수행한다.
8. 가장 싼 다음 검증 방법을 제안한다.

## UI/UX
- Mobile First.
- 기본 기준 390×844.
- 360×800, 390×844, 430×932에서 검증.
- Web 기준 1440×900.
- Toss-inspired: 단순함, 명확한 정보 계층, 넉넉한 여백, 빠른 행동.
- 특정 서비스 화면을 그대로 복제하지 않는다.
- 화면마다 Primary CTA는 원칙적으로 1개.
- 장식용 카드, KPI 박스, 배너, 탭을 임의 추가하지 않는다.
- Bold 남발 금지. Medium 중심.
- 가로 스크롤 금지.

## PM 품질 기준
- 사용자에게 없는 수치나 사실을 꾸며내지 않는다.
- 추정은 `가정`, 미확인은 `확인 필요`로 표시한다.
- 목표와 비목표를 함께 정의한다.
- MVP보다 싼 검증법이 있으면 먼저 제안한다.
- 성공지표와 guardrail을 구분한다.
- 구현 전 결정이 필요한 사항을 숨기지 않는다.

## 안전
- 사용자 승인 없는 실배포, 실메일, 실게시, 결제, 삭제 금지.
- 비밀키와 .env 값 출력 금지.
- 프로토타입은 mock data / local 우선.
- Vercel 연결이나 Production deploy를 자동 수행하지 않는다.

## Git
- `main`은 안정 버전.
- 기능 단위 branch 사용: `feat/...`, `fix/...`, `docs/...`, `refactor/...`.
- unrelated change를 한 branch에 섞지 않는다.
- PR에는 변경 이유와 검증 결과를 적는다.
- 기본 merge는 squash merge.
